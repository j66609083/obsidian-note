安装命令：`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh`
卸载命令：`rustup self uninstall`

toolchain是什么？

默认会下载6个组件：
`cargo`
`clippy`
`rust-docs`
`rust-std`
`rustc`
`rustfmt`


使用以下检查安装是否成功：
```bash
rustc --version
```