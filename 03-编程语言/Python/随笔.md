
模块组织代码，包组织模块
API用Pydantic，内部用dataclass，简单场景用class
metaclass是用来创建类的类，可以拦截类创建

框架 API 层允许多态输入，但会通过 normalization/adaptation layer 统一转换为内部标准对象，从而避免业务逻辑层充斥类型分支

函数不能直接当父类，但函数对象可以通过 `__mro_entries__` 在类创建时“伪装成基类入口”，最终替换成真正的类。

### 鸭子类型（duck typing）